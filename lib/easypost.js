import { toOz } from './format';
function apiKey(){const k=process.env.EASYPOST_API_KEY;if(!k)throw new Error('Missing EASYPOST_API_KEY');return k}
function auth(){return 'Basic '+Buffer.from(`${apiKey()}:`).toString('base64')}
async function epFetch(path,options={}){const res=await fetch(`https://api.easypost.com/v2${path}`,{...options,headers:{Authorization:auth(),'Content-Type':'application/json',...(options.headers||{})},cache:'no-store'});const text=await res.text();let data;try{data=text?JSON.parse(text):{}}catch{data={raw:text}}if(!res.ok)throw new Error(`EasyPost API failed: ${res.status} ${JSON.stringify(data)}`);return data}
export function buildShipmentPayload(order={}){const weight=order.parcel_weight||toOz(order.parcel_weight_lb,order.parcel_weight_oz)||16;return {shipment:{to_address:{name:order.customer_name,street1:order.address_line1,street2:order.address_line2||undefined,city:order.city,state:order.state,zip:order.zip,country:order.country||'US',phone:order.customer_phone||undefined,email:order.customer_email||undefined},from_address:{company:"Erendira's Boutique",street1:'17121 Hawthorne Ave',city:'Fontana',state:'CA',zip:'92335',country:'US',phone:process.env.SHIP_FROM_PHONE||undefined,email:process.env.SHIP_FROM_EMAIL||undefined},parcel:{length:Number(order.parcel_length||13),width:Number(order.parcel_width||10),height:Number(order.parcel_height||10),weight:Number(weight)},options:{label_format:'PDF',label_size:'4x6',print_custom_1:order.customer_name||undefined,delivery_confirmation:order.signature_required?'SIGNATURE':undefined},reference:order.id||undefined}}}
export const createEasyPostShipment=(order)=>epFetch('/shipments',{method:'POST',body:JSON.stringify(buildShipmentPayload(order))})
export const buyEasyPostLabel=(shipmentId,rateId)=>epFetch(`/shipments/${shipmentId}/buy`,{method:'POST',body:JSON.stringify({rate:{id:rateId}})})
export const refundEasyPostShipment=(shipmentId)=>epFetch(`/shipments/${shipmentId}/refund`,{method:'POST',body:JSON.stringify({})})
