export function formatAddress(order={}){return [order.address_line1,order.address_line2,[order.city,order.state,order.zip].filter(Boolean).join(', ')].filter(Boolean).join('\n')}
export function displayDate(v){if(!v)return new Date().toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric'});try{return new Date(v).toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric'})}catch{return v}}
export function notificationText(order={}){const carrier=order.carrier||'the shipping carrier';return `A package was shipped to you via ${carrier} and will be delivered to:\n\n${order.customer_name||'Customer'}\n${order.customer_address||formatAddress(order)}\n\nShipment Date: ${order.shipment_date||displayDate(order.ship_date)}\nMail Class: ${order.mail_class||'Shipping Service'}\nTracking Number: ${order.tracking_number||''}\n\nCheck the package status:\nhttps://track.erendirasboutique.com/?tracking=${order.tracking_number||''}\n\nFor questions about this package, please contact us or ${carrier}.`}
export function normalizeOrderRow(row={}){return {...row,customer_address:row.customer_address||formatAddress(row)}}
