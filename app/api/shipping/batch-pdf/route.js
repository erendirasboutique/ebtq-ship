import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { isShippingAuthenticated } from '@/lib/auth';import { selectOrders } from '@/lib/supabaseRest';import { PDFDocument } from 'pdf-lib';
export async function POST(req){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});try{const {ids=[]}=await req.json();const orders=(await selectOrders()).filter(o=>ids.includes(o.id)&&o.label_url);if(!orders.length)throw new Error('No label PDFs selected.');const merged=await PDFDocument.create();for(const order of orders){const res=await fetch(order.label_url);if(!res.ok)continue;const bytes=await res.arrayBuffer();const src=await PDFDocument.load(bytes);const pages=await merged.copyPages(src,src.getPageIndices());pages.forEach(p=>merged.addPage(p))}const out=await merged.save();return new NextResponse(Buffer.from(out),{headers:{'Content-Type':'application/pdf','Content-Disposition':'inline; filename="erendiras-batch-labels.pdf"'}})}catch(e){return NextResponse.json({error:e.message},{status:500})}}
