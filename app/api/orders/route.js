import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { isShippingAuthenticated } from '@/lib/auth';import { selectOrders, upsertOrder } from '@/lib/supabaseRest';import { fullAddress, toOz } from '@/lib/format';
export async function GET(){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({orders:await selectOrders()})}
export async function POST(req){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await req.json();const row={...b,parcel_length:b.parcel_length||13,parcel_width:b.parcel_width||10,parcel_height:b.parcel_height||10,parcel_weight:toOz(b.parcel_weight_lb,b.parcel_weight_oz)||b.parcel_weight||16,customer_address:b.customer_address||fullAddress(b),status:'draft'};const saved=await upsertOrder(row);return NextResponse.json({ok:true,order:saved,orders:await selectOrders()})}catch(e){return NextResponse.json({error:e.message},{status:500})}}
