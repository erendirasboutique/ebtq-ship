import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { isShippingAuthenticated } from '@/lib/auth';import { PDFDocument } from 'pdf-lib';
export async function POST(req){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});try{const {labelUrls=[]}=await req.json();const merged=await PDFDocument.create();for(const url of labelUrls.filter(Boolean)){const res=await fetch(url);const bytes=await res.arrayBuffer();const pdf=await PDFDocument.load(bytes);const pages=await merged.copyPages(pdf,pdf.getPageIndices());pages.forEach(p=>merged.addPage(p))}const out=await merged.save();return new NextResponse(Buffer.from(out),{headers:{'Content-Type':'application/pdf','Content-Disposition':'inline; filename="erendiras-batch-labels.pdf"'}})}catch(e){return NextResponse.json({error:e.message},{status:500})}}
