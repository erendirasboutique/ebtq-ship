import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { isShippingAuthenticated } from '@/lib/auth';
export async function POST(req){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});const {urls=[]}=await req.json();return NextResponse.json({ok:true,urls,message:'Open each label URL and print together from your browser. PDF merging can be added later.'})}
