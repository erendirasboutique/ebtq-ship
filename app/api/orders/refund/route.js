import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { isShippingAuthenticated } from '@/lib/auth';import { refundEasyPostShipment } from '@/lib/easypost';import { getOrder, updateOrder, selectOrders } from '@/lib/supabaseRest';
export async function POST(req){const c=await cookies();if(!isShippingAuthenticated(c))return NextResponse.json({error:'Unauthorized'},{status:401});try{const {id}=await req.json();const order=await getOrder(id);const refund=await refundEasyPostShipment(order.easypost_shipment_id);await updateOrder(id,{status:'refund_requested',notes:[order.notes,JSON.stringify(refund)].filter(Boolean).join('\n')});return NextResponse.json({ok:true,orders:await selectOrders()})}catch(e){return NextResponse.json({error:e.message},{status:500})}}
