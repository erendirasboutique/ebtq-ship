import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { isShippingAuthenticated } from '@/lib/auth';import { selectCustomers } from '@/lib/supabaseRest';import Header from '@/components/Header';import CustomersDashboard from '@/components/CustomersDashboard';
export const dynamic='force-dynamic';
export default async function Page(){const c=await cookies();if(!isShippingAuthenticated(c))redirect('/login');let customers=[];try{customers=await selectCustomers()}catch(e){}return <div className="page"><div className="shell"><Header/><CustomersDashboard initialCustomers={customers}/></div></div>}
