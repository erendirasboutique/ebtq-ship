import { cookies } from 'next/headers';import { redirect } from 'next/navigation';import { isShippingAuthenticated } from '@/lib/auth';import { selectCustomers, selectOrders } from '@/lib/supabaseRest';import Header from '@/components/Header';import Customers from '@/components/Customers';
export const dynamic='force-dynamic';
export default async function Page(){const c=await cookies();if(!isShippingAuthenticated(c))redirect('/login');let customers=[],orders=[];try{customers=await selectCustomers();orders=await selectOrders()}catch{}return <div className="page"><div className="shell"><Header/><Customers initialCustomers={customers} initialOrders={orders}/></div></div>}
