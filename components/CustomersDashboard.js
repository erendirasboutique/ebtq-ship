'use client';
import { useMemo, useState } from 'react';
function parseCSV(text){const rows=[];let row=[],cur='',q=false;for(let i=0;i<text.length;i++){const ch=text[i],n=text[i+1];if(ch==='"'&&q&&n==='"'){cur+='"';i++}else if(ch==='"'){q=!q}else if(ch===','&&!q){row.push(cur);cur=''}else if((ch==='\n'||ch==='\r')&&!q){if(ch==='\r'&&n==='\n')i++;row.push(cur);if(row.some(v=>v.trim()))rows.push(row);row=[];cur=''}else cur+=ch}row.push(cur);if(row.some(v=>v.trim()))rows.push(row);const headers=(rows.shift()||[]).map(h=>h.trim());return rows.map(r=>Object.fromEntries(headers.map((h,i)=>[h,r[i]||''])))}
export default function CustomersDashboard({initialCustomers=[]}){const[customers,setCustomers]=useState(initialCustomers);const[q,setQ]=useState('');const[editing,setEditing]=useState(null);const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const filtered=useMemo(()=>{const t=q.toLowerCase().trim();return customers.filter(c=>!t||[c.customer_name,c.email,c.phone,c.customer_address,c.zip].join(' ').toLowerCase().includes(t))},[q,customers]);async function importFile(e){const file=e.target.files?.[0];if(!file)return;setBusy(true);setMsg('Importing customers...');try{const rows=parseCSV(await file.text());const res=await fetch('/api/customers/import',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rows})});const data=await res.json();if(!res.ok)throw new Error(data.error||'Import failed');setCustomers(data.customers||[]);setMsg(`Imported ${data.imported||0}. Skipped duplicates: ${data.skipped_duplicates||0}.`)}catch(err){setMsg(err.message)}finally{setBusy(false);e.target.value=''}}async function saveCustomer(e){e.preventDefault();setBusy(true);setMsg('Saving customer...');try{const res=await fetch('/api/customers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(editing)});const data=await res.json();if(!res.ok)throw new Error(data.error||'Save failed');setCustomers(data.customers||customers);setEditing(null);setMsg('Customer saved.')}catch(err){setMsg(err.message)}finally{setBusy(false)}}return <main className="card"><h2>Customers</h2><p className="muted">Import your CSV, manually add customers, and edit addresses anytime.</p>{msg&&<div className="notice">{msg}</div>}<div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search customers..."/><label className="btn ghost" style={{display:'inline-flex'}}>Import CSV<input type="file" accept=".csv" onChange={importFile} style={{display:'none'}} disabled={busy}/></label><button className="btn primary" onClick={()=>setEditing({customer_name:'',email:'',phone:'',address_line1:'',address_line2:'',city:'',state:'CA',zip:'',country:'US'})}>Add Customer</button></div><div className="customer-grid">{filtered.map(c=><button key={c.id||c.import_key} className="customer" onClick={()=>setEditing(c)}><b>{c.customer_name}</b><br/><span className="small">{c.email||c.phone||'No contact'}</span><div className="address">{c.customer_address||[c.address_line1,c.address_line2,[c.city,c.state,c.zip].filter(Boolean).join(', ')].filter(Boolean).join('\n')}</div></button>)}</div>{editing&&<div className="modal-backdrop"><form className="modal" onSubmit={saveCustomer}><button type="button" className="close" onClick={()=>setEditing(null)}>×</button><h2>{editing.id?'Edit Customer':'Add Customer'}</h2><div className="section-grid"><label className="wide">Name<input value={editing.customer_name||''} onChange={e=>setEditing({...editing,customer_name:e.target.value})} required/></label><label>Email<input value={editing.email||editing.customer_email||''} onChange={e=>setEditing({...editing,email:e.target.value,customer_email:e.target.value})}/></label><label>Phone<input value={editing.phone||editing.customer_phone||''} onChange={e=>setEditing({...editing,phone:e.target.value,customer_phone:e.target.value})}/></label><label className="wide">Address 1<input value={editing.address_line1||''} onChange={e=>setEditing({...editing,address_line1:e.target.value})}/></label><label className="wide">Address 2<input value={editing.address_line2||''} onChange={e=>setEditing({...editing,address_line2:e.target.value})}/></label><label>City<input value={editing.city||''} onChange={e=>setEditing({...editing,city:e.target.value})}/></label><label>State<input value={editing.state||''} onChange={e=>setEditing({...editing,state:e.target.value})}/></label><label>ZIP<input value={editing.zip||''} onChange={e=>setEditing({...editing,zip:e.target.value})}/></label><label>Country<input value={editing.country||'US'} onChange={e=>setEditing({...editing,country:e.target.value})}/></label></div><div className="actions" style={{marginTop:16}}><button className="btn primary" disabled={busy}>Save Customer</button></div></form></div>}</main>}
