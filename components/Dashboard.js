'use client';import { notificationText } from '@/lib/format';
export default function Dashboard({orders=[],customers=[],loadError=''}){async function copy(o){await navigator.clipboard.writeText(notificationText(o));alert('Notification copied')}return <><section className="grid"><div className="stat glass"><b>{orders.length}</b><span>Total Orders</span></div><div className="stat glass"><b>{customers.length}</b><span>Customers</span></div><div className="stat glass"><b>{orders.filter(o=>o.label_url).length}</b><span>Labels</span></div><div className="stat glass"><b>{new Date().toLocaleDateString('en-US',{month:'short',day:'numeric'})}</b><span>Today</span></div></section>{loadError&&<div className="notice error">{loadError}</div>}<section className="card glass"><h2>Recent Orders</h2><div className="list">{orders.slice(0,10).map(o=><div className="item" key={o.id}><h3>{o.customer_name||'Customer'}</h3><div className="pills"><span className="pill">{o.carrier||'No carrier yet'}</span><span className="pill">{o.mail_class||'No service yet'}</span><span className="pill">{o.status||'draft'}</span></div><p className="address">{o.customer_address}</p><div className="row">{o.label_url&&<a className="btn ghost" href={o.label_url} target="_blank">Print Label</a>}{o.tracking_number&&<button className="btn green" onClick={()=>copy(o)}>Copy Notification</button>}</div></div>)}{!orders.length&&<div className="empty">No orders yet. Create your first label.</div>}</div></section></>}
