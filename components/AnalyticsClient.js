export default function AnalyticsClient({orders=[]}){const labels=orders.filter(o=>o.tracking_number).length;const drafts=orders.length-labels;const carriers=orders.reduce((a,o)=>{const k=o.carrier||'Unknown';a[k]=(a[k]||0)+1;return a},{});return <section className="layout"><div className="card glass"><h2>Shipping Summary</h2><table className="table"><tbody><tr><th>Total Orders</th><td>{orders.length}</td></tr><tr><th>Labels Bought</th><td>{labels}</td></tr><tr><th>Drafts</th><td>{drafts}</td></tr><tr><th>Notifications Waiting</th><td>{orders.filter(o=>!o.notification_sent).length}</td></tr></tbody></table></div><div className="card glass"><h2>Carrier Mix</h2><div className="list">{Object.entries(carriers).map(([k,v])=><div className="order-card" key={k}><div className="order-head"><h3>{k}</h3><span className="pill">{v}</span></div></div>)}{!orders.length&&<div className="empty">No data yet.</div>}</div></div></section>}
