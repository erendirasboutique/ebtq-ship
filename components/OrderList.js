'use client';
import { useMemo, useState } from 'react';
import OrderModal from './OrderModal';
export default function OrderList({initialOrders=[]}){const[orders,setOrders]=useState(initialOrders);const[q,setQ]=useState('');const[selected,setSelected]=useState(null);const[msg,setMsg]=useState('');const[batch,setBatch]=useState([]);const filtered=useMemo(()=>{const t=q.toLowerCase();return orders.filter(o=>!t||[o.customer_name,o.customer_address,o.tracking_number,o.carrier,o.mail_class,o.status].join(' ').toLowerCase().includes(t))},[q,orders]);async function refund(order){if(!confirm('Request refund/void for this label?'))return;setMsg('Requesting refund...');const res=await fetch('/api/shipping/refund-label',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({order})});const data=await res.json();if(!res.ok)setMsg(data.error);else{setOrders(data.orders);setSelected(data.orders.find(x=>x.id===order.id));setMsg('Refund request submitted.')}}async function printBatch(){const urls=orders.filter(o=>batch.includes(o.id)&&o.label_url).map(o=>o.label_url);const res=await fetch('/api/shipping/batch-pdf',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({urls})});if(!res.ok){const d=await res.json();setMsg(d.error);return}const blob=await res.blob();window.open(URL.createObjectURL(blob),'_blank')}return <><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search orders, customers, tracking..."/>{batch.length>0&&<button className="btn primary" onClick={printBatch}>Print Batch ({batch.length})</button>}</div>{msg&&<div className="notice">{msg}</div>}<div className="list">{!filtered.length&&<div className="empty card">No orders found.</div>}{filtered.map(o=><article className="order-card" key={o.id} onClick={()=>setSelected(o)}><div className="order-head"><div><h3>{o.customer_name||'Customer'}</h3><p className="muted">{o.customer_email||o.customer_phone||'No contact saved'}</p></div><input type="checkbox" checked={batch.includes(o.id)} onClick={e=>e.stopPropagation()} onChange={e=>setBatch(v=>e.target.checked?[...v,o.id]:v.filter(id=>id!==o.id))}/></div><div className="pills"><span className="pill">{o.status||'draft'}</span><span className="pill">{o.carrier||'No carrier'}</span><span className="pill">{o.mail_class||'No service'}</span><span className="pill">{o.source||'easypost'}</span></div><div className="address">{o.customer_address}</div><p><b>Tracking:</b> {o.tracking_number||'—'}</p><div className="row">{o.label_url&&<a className="btn ghost" href={o.label_url} target="_blank" onClick={e=>e.stopPropagation()}>Print Label</a>}</div></article>)}</div><OrderModal order={selected} onClose={()=>setSelected(null)} onRefund={refund}/></>}
